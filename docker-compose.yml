version: "1.1"

services:
  reactjs:
    ports:
      - 6000:5000
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/build
